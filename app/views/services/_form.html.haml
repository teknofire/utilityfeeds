= simple_form_for(@service) do |f|
  = f.error_notification
  .form-inputs
    = f.input :name
    = f.input :color, hint: '#RRGGBB'
  .form-actions.text-right
    = f.button :submit, class: 'btn btn-primary'
